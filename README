Dependencies:
npx expo install expo-file-system
npx expo install expo-av
npx expo install expo-sqlite
npx expo install expo-router expo-linking expo-constants expo-status-bar react-native-safe-area-context react-native-screens

for some reason i cpuld not get the first sound to play from recorded audio but every recording after that works fine.
